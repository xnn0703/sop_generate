# 更新日志

版本号管理在 [`core/__init__.py`](core/__init__.py) 的 `__version__`，所有地方（GUI 标题 / CLI `--version` / 打包元信息）从同一处读取。

---

## v1.2.4 — 2026-07-02

### 修复
- 修复新增/插入/删除/排序工序后，右侧 HTML 预览可能继续显示旧页面的问题；预览刷新现在会强制重新加载最新临时 HTML，并继续定位到当前工序页。
- 修复导出 HTML / PDF 失败的问题，导出时正确读取 `sop_packages/<型号>/images/` 图片目录。

### 发布
- Windows 发版分卷改为约 10MB 一卷，降低 Gitee 附件上传超时概率。
- 新增 Gitee Release 分卷补传流程，便于某个分卷漏传时单独修复。

### 文档
- `docs/使用说明.html` 同步版本号，并将下载说明改为“所有分卷”而非固定 5 个分卷。

---

## v1.2.3 — 2026-07-02

### 修复
- 工序列表拖拽彻底移除 Qt 自带 DragDrop/InternalMove 链路，改为鼠标按下/松开时由软件直接计算源工序和目标行，避免 macOS 上仍出现移动错行、编号跳变的问题。
- 点击左侧工序时，右侧 HTML 预览现在会主动滚动到对应工序页；刷新预览时也会带上工序锚点。

### 发布
- GitHub Actions 发版改为仅构建和上传 Windows 包；macOS 包后续按需在本地运行 `packaging/build_macos.sh` 生成。

### 文档
- `docs/使用说明.html` 同步版本号。

---

## v1.2.2 — 2026-07-02

### 修复
- 修复 macOS 上工序拖拽排序仍可能移动错行的问题：拖拽源工序 ID 现在写入拖拽数据包，放下时只按该 ID 定位真实工序，不再回退读取当前选中行。
- 修复拖拽 demo 工序时可能出现编号跳变、某道工序暂时不显示的问题。

### 文档
- `docs/使用说明.html` 同步版本号。

---

## v1.2.1 — 2026-07-02

### 改进
- 图片布局编辑器改为直接拖动图片本体，选中时显示缩放点，减少外框干扰。
- 图片缩放预览始终从原图重采样，避免拖拽/缩放过程中明显模糊。
- 图片手动布局新增左转/右转 90°，布局字段新增 `rotation: 0/90/180/270`，旧工程默认按 0° 处理。
- 刷新 HTML 预览后自动回到左侧当前选中的工序页，不再每次跳回封面。

### 修复
- 修复拖拽调整工序顺序时 Qt 列表显示与真实 `processes` 数据可能错位的问题。
- 修复交换靠后的工序（如 13/14）后可能出现行消失、编号不重排的问题。
- 重名工序拖拽按真实工序对象移动，不再依赖显示文本或临时行号。

### 文档
- `docs/使用说明.html` 同步图片直接拖动、旋转、预览定位和工序拖拽排序说明。
- `sop_packages/_schema.yaml` 同步图片布局 `rotation` 字段。

---

## v1.2.0 — 2026-07-01

### 新功能
- 支持最多三级工序结构，自动生成 `3`、`3-1`、`3-1-1` 格式编号。
- 流程图升级为 **主流程 + 组内展开**，复杂父级工序可展示子工序。
- 工序详情表头新增 **工序编号** 和 **标准工时**，工时按分钟输入并自动显示为 `45min`、`1h`、`1h15min`。
- 新增图片布局编辑器，可在固定图片画布内拖拽、缩放图片，并保存百分比位置/尺寸。

### 改进
- 父级工序有内容时显示完整详情页；父级无内容但有子级时显示摘要页；空的末级工序显示占位详情页。
- 保存、预览、HTML/PDF 导出允许工序内容为空；归档定稿遇到草稿内容时二次确认。
- 旧 `images: ["a.png"]` 继续兼容，新图片条目支持 `{file, layout}`。

### 文档
- `docs/使用说明.html`、`sop_packages/_schema.yaml` 同步多级工序、工时、图片布局和草稿导出说明。

### 升级注意
- v1.1.x 工程可直接打开，缺省按一级工序处理；旧图片列表自动按原自动布局显示。

---

## v1.1.1 — 2026-06-30

### 新功能
- 新增 **插入工序**：可把新工序插入到选中工序前面或后面，后续编号自动重排。

### 修复
- 修复拖拽调整工序顺序后，只改变左侧显示、不改变实际 SOP 数据的问题。
- 修复纯拖拽排序后保存时，工序 `last_modified_by` 被误改成当前用户的问题。
- 修复自动更新后旧 `products/` 残留导致每次启动都弹"检测到旧版本数据"的问题。
- 修复自动更新保留目录仍停留在 v1.0.x 结构的问题，升级时现在会保留 `sop_packages/`、`config/`、`output/`、`_legacy_v1_backup/`。

### 文档
- `docs/使用说明.html` 同步新增工序追加/插入/拖拽排序说明，并修正自动更新保留数据说明。

### 发布
- Windows / macOS 仍并行打包，但发布分卷统一由单独 job 串行上传到 Gitee / GitHub Release，避免 Gitee 并发附件上传卡住。
- Gitee 分卷上传增加低速超时、重试和片间等待，避免单个网络请求长时间挂起或连续上传卡住。

### 升级注意
- v1.1.0 用户可直接自动更新到 v1.1.1；本版本会保留现有 SOP 工程和用户名配置。

---

## v1.1.0 — 2026-05-30

**重大版本：架构重构 + 协作支持**

### 新功能
- 🆕 **独立 SOP 工程包结构**：每个 SOP 自包含一个工程文件夹
  ```
  sop_packages/<MODEL>/
  ├── product.yaml      （工序数据 + _meta 修改追溯）
  ├── images/           （工序图）
  └── output/           （生成的 HTML/PDF）
  ```
- 🆕 **修改追溯**：每道工序自动记录 created_by/at + last_modified_by/at
  - 启动时强制要求用户名（保存在 `config/current_user.json`）
  - 工序列表显示 · 最后修改人
  - 工序编辑器底部显示完整创建/修改信息
  - 保存时智能对比，只更新真正改动过的段
- 🆕 **.sopkg 包导入/导出**：一键打包整个 SOP 工程为 .sopkg（zip），方便发给同事接力编辑
  - 同名导入自动备份为 `.bak_<时间戳>/`
- 🆕 **📦 归档定稿**：定稿后一键生成带日期的归档物到指定文件夹
  - 工程文件夹（YAML + 图片 + HTML + PDF）
  - 同名 .sopkg 单文件包
  - 同日多次自动加序号，不覆盖
- 🆕 **删除 SOP 功能**：双重确认（警告弹窗 + 手动输入型号）防误删
- 🆕 **帮助按钮**：显示版本号、当前用户、打开使用手册、检查更新
- 🆕 **自动迁移**：首次启动检测 v1.0.x 数据，弹窗一键迁移到新结构（老数据备份到 `_legacy_v1_backup/`，保留 1 个月）

### 改进
- ⚙️ **移除所有字段数量硬性限制**：operations / notes / tools / materials / images 都支持任意数量，超出单页容量自动"齐头分页"显示
- ⚙️ **移除 model 字符集限制**：现在支持中文等任意语言，仅禁文件系统不允许字符（`\ / : * ? " < > |`）
- ⚙️ **取消"工序至少 1 张图"硬性要求**：允许工序没有图片
- 🎨 **软件标题简化**：从 `sop_generate v1.0.8 · 作业指导书生成器` → `SOP Generate · 当前用户：xxx`
- 📖 **使用说明 HTML 全面重写**：19 章节，含真实主界面截图，按 OS 分组的下载链接
- 📝 新增 `CLAUDE.md`：项目硬性规则（用户可见改动必须同步使用说明）

### CLI
- `python gen.py DEMO01` （按型号名，推荐）
- `python gen.py sop_packages/DEMO01/` （工程文件夹）
- `python gen.py products/DEMO01.yaml` （v1.0.x 兼容路径，仍可用）

### 升级注意
- v1.0.x 用户首次启动 v1.1.0 会自动弹窗迁移老数据，**点立即迁移**即可（数据备份在 `_legacy_v1_backup/`，1 个月内可回滚）
- 升级后软件目录结构变化：`products/` + `assets/images/` → `sop_packages/<MODEL>/`

---

## v1.0.8 — 2026-05-29

- 自动更新闭环验证版本：v1.0.7 → v1.0.8 升级
  - 新增的 Content-Length 校验 + 重试 + 7z 魔数校验全链路应该一次成功
  - 无功能改动，仅版本号 +1

---

## v1.0.7 — 2026-05-29

**修复 Bug**：自动更新 `Bad7zFile: invalid header data`

- 旧版 updater 下载分卷时无 Content-Length 校验、无重试，网络抖动会留下截断文件
- 客户端合并后的 .7z 文件头不对，py7zr 报魔数错误
- 修复：
  - `_download_one()` 单文件下载，校验 Content-Length，失败自动重试 4 次（指数退避 2s/4s/8s/10s）
  - 中途失败的文件自动删除避免污染
  - 合并后立即校验 7z 魔数（37 7a bc af 27 1c），早报错给用户清晰提示
- ⚠️ v1.0.5 / v1.0.6 客户端自身的下载逻辑无修复，无法自动跨过这版升级；需**手动重装** v1.0.7

---

## v1.0.6 — 2026-05-29

- 自动更新端到端验证版本：从 v1.0.5 自动升级到 v1.0.6 应当一气呵成
  - updater self-relocation 到临时目录
  - 替换 sop_generate.exe / updater.exe / _internal/
  - 保留 products / assets / output 用户数据
  - 自动启动新版本主程序

---

## v1.0.5 — 2026-05-29

**修复关键 Bug**：Windows 自动更新失败 `PermissionError: [WinError 5] 拒绝访问: updater.exe`

- Windows 上运行中的 .exe 不能被删除（与 Linux/Mac 行为不同）
- updater.exe 试图替换自己所在目录的文件 → 删自己失败
- 修复：把之前 macOS 才有的 self-relocation 扩展到 Windows：
  - 启动时把 updater.exe + _internal/ 复制到 %TEMP%/sop_updater_clone_<ts>/
  - 从临时目录跑，原 updater.exe 就能被替换
  - 退出 30 秒后由 cmd 异步清理临时目录
- v1.0.2 / v1.0.3 / v1.0.4 用户需**手动**重装本版本（旧 updater 无此修复，无法自动跨过这步）

---

## v1.0.4 — 2026-05-29

**修复关键 Bug**：自动更新解压失败 `UnsupportedCompressionMethodError: BCJ2 filter is not supported by py7zr`

- 7-Zip `-mx=9` 极限压缩对 x86 可执行文件默认启用 BCJ2 滤镜（提升压缩率），但客户端 py7zr 1.1.x 不支持解 BCJ2
- CI 加 `-mf=off` 禁用执行过滤器：体积增加约 5-10%，换来 py7zr 兼容
- v1.0.2 / v1.0.3 客户端从这版起可以正常自动更新

---

## v1.0.3 — 2026-05-29

- **多图布局扩展**：图片上限 2 → **4** 张
  - 1 张：全占工序页右半区
  - 2 张：上下排列
  - 3-4 张：2×2 网格自动缩放（不再被截断）
- **CI 双平台并行**：拆出 `prepare-release` job（轻量 Linux）先建空 Release，
  Windows 与 macOS 在创建好的 Release 上并行追加分卷。
  墙钟从 ~3 小时（串行）降到 ~80 分钟（并行）

---

## v1.0.2 — 2026-05-28

- **macOS 自动更新升级为 self-relocation 模式**：updater 启动时把整个 .app 复制到 /tmp 跑，
  完成后原地替换 .app（不再留隔壁旧目录）；/tmp 副本 30 秒后自动清理
- macOS 升级体验现在与 Windows 一致：原地替换 → 启动新版本，无残留旧目录
- 双平台 CI（windows + macOS）：tag 触发时同时构建 .exe 和 .app，分卷上传到同一 Gitee Release
- 客户端按当前 OS 自动筛选下载对应平台的分卷

---

## v1.0.1 — 2026-05-28

- 新增**自动更新**机制（独立 updater 子程序 + Gitee Release 增量分发）
- 双 EXE 打包（PyInstaller MERGE 共享运行时，增量体积仅 14MB）
- 启动时后台检查更新，弹窗"立即更新 / 暂不 / 跳过此版本"
- 升级时自动保留 `products/` / `assets/` / `output/` 用户数据
- 改用独立公开 Gitee 仓库 `sop_generate-releases` 分发，主仓可保持私有
- 通过 `release.config.json` 配置发版仓库地址，CI 自动从 Secrets 注入
- 移除所有业务信息硬编码（公司名、产品代号等改为占位）

---

## v1.0.0 — 2026-05-28

首发版本。

### 核心功能
- **CLI** (`gen.py`)：YAML → HTML / PDF，支持批量与校验
- **PySide6 GUI**：左侧产品/工序树、中间编辑器、右侧实时预览，支持工序拖拽排序、图片拖入
- **Claude Code Skill**：自然语言描述需求 → Claude 起草/修改 YAML 并触发生成

### 文档排版
- **A3 大小**：封面 / 目录 / 工艺流程图（纵向 297×420mm），工序详情页（横向 420×297mm）
- **表格铺满**：工序详情页表格充满页面可用区
- **工序自动拆页**：operations 超 6 条自动拆成多页，工序号保持一致，标"续 N/M"
- **工艺流程图分页**：超 8 个节点自动续到下一页，加"接上页 / 续下页"提示
- **拆页时图片每页都显示**（注意事项 / 工具 / 材料仅首页）

### 字段约束
- processes 1-32 项
- operations 1-18 条（自动拆页）
- notes / tools / materials 0-4 项
- images 1-2 张（多图建议拆工序）
- 字符长度无硬限，超长由 CSS overflow:hidden 兜底

### 数据迁移
- GUI 工具栏新增 **"导入旧数据"** 按钮，从老版本目录一键迁移所有 YAML + 图片
- 同名文件跳过不覆盖，保护当前数据

### 打包发布
- macOS `.app` + Windows `.exe`（PyInstaller 跨平台 spec）
- 自定义应用图标（文档+齿轮+对勾+SOP 渐变设计）
- GitHub Actions windows-latest runner 自动出 Windows 版
- 打 tag 时 7z 极限压缩 + 30MB 分卷上传 Gitee Release（国内快速下载）+ 同步建 GitHub Release

### 代码托管
- 主仓与发版位置见 `release.config.json`
- `git push` 同时推到所有配置的远程
